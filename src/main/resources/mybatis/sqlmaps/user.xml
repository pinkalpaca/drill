<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.ondony">
    <select id="getUserList" resultType="com.test.ondony.model.User">
        SELECT
            idx,
            name,
            dept
        FROM
            user
    </select>
</mapper>